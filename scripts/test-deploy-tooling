#!/usr/bin/env bash
set -euo pipefail

REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

"${REPO_DIR}/scripts/tests/check-service-inventory"
"${REPO_DIR}/scripts/tests/check-secret-rendering"
"${REPO_DIR}/scripts/tests/check-service-rollout"
"${REPO_DIR}/scripts/tests/check-redeploy-image"
"${REPO_DIR}/scripts/tests/check-deploy-guards"

echo "Deploy tooling checks passed."
