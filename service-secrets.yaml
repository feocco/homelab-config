services:
  hello-nas:
    env:
      - HELLO_NAS_MESSAGE
      - HOST_PORT

  plant-monitor:
    env:
      - HA_URL
      - HA_LONG_LIVED_TOKEN
      - TZ
      - LOG_LEVEL
      - HA_NOTIFY_SERVICE
      - HA_PLANTS_DASHBOARD_URL
      - ALERT_SNOOZE_HOURS
      - ALERT_REPEAT_HOURS
      - SERVICE_HOST
      - SERVICE_PORT
      - SERVICE_CALLBACK_TOKEN
      - CONFIG_PATH
      - STATE_PATH
      - DRY_RUN
