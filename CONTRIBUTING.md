# Contributing

This repository is a **read-only architecture showcase**.

- Viewing and linking are welcome.
- Pull requests, issues, and patches are not accepted.
- It is not a template for deploying someone else's homelab.

Operator notes under `docs/` assume access to private hosts, Actions secrets,
and host-local overlays that are intentionally not in git.
